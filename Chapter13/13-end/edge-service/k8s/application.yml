spring:
  config:
    import: optional:configserver:,configtree:/workspace/secrets/*/
  cloud:
    bus:
      enabled: true
    config:
      uri: http://config-service
  rabbitmq:
    host: polar-rabbitmq
  redis:
    host: polar-redis